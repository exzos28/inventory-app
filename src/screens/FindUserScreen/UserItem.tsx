import React from 'react';
import {ListItem} from '@ui-kitten/components';
import {observer} from 'mobx-react-lite';

export default observer(() => (
  <ListItem title="First name / Last name" description="Description" />
));
